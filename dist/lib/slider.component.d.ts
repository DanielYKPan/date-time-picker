import { OnInit, ElementRef, EventEmitter, Renderer2, OnDestroy } from '@angular/core';
export declare class SlideControlComponent implements OnInit, OnDestroy {
    private el;
    private renderer;
    private movePointer;
    private stopPointer;
    private mouseMoveListener;
    private mouseUpListener;
    private touchMoveListener;
    private touchEndListener;
    step: number;
    floor: number;
    ceiling: number;
    precision: number;
    low: number;
    theme: string;
    lowChange: EventEmitter<number>;
    bar: ElementRef;
    highlight: ElementRef;
    lowPointer: ElementRef;
    private pointerHalfWidth;
    private barWidth;
    private minOffset;
    private maxOffset;
    private minValue;
    private maxValue;
    private valueRange;
    private offsetRange;
    constructor(el: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    ngOnDestroy(): void;
    private setPointers();
    private start(event);
    private stop();
    private move(event);
    private getX(event);
    private roundStep(value, precision, step, floor);
    private contain(value);
    private percentValue(value);
    private percentOffset(offset);
    private pixelsToOffset(percent);
}
