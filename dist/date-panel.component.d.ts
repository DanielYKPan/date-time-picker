import { OnInit, EventEmitter, OnChanges, SimpleChanges, OnDestroy } from '@angular/core';
import * as moment from 'moment/moment';
import { Moment } from 'moment/moment';
import { DialogType } from './dialog.component';
import { PickerService } from './picker.service';
export declare class DatePanelComponent implements OnInit, OnChanges, OnDestroy {
    private service;
    dialogType: DialogType;
    onDialogTypeChange: EventEmitter<DialogType>;
    onClosePicker: EventEmitter<boolean>;
    onConfirm: EventEmitter<boolean>;
    onSelected: EventEmitter<moment.Moment>;
    autoClose: boolean;
    type: DialogType;
    now: Moment;
    calendarMoment: Moment;
    calendarDays: Moment[];
    dayNames: string[];
    monthList: string[];
    yearList: number[];
    mode: 'popup' | 'dropdown' | 'inline';
    onlyCurrentMonth: boolean;
    private locale;
    private subId;
    private momentFunc;
    constructor(service: PickerService);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    prevMonth(): void;
    nextMonth(): void;
    selectMonth(month: string): void;
    selectYear(year: number): void;
    toggleDialogType(type: DialogType): void;
    generateYearList(param?: string): void;
    select(moment: Moment): void;
    confirm(): void;
    closePicker(): void;
    private setCalendarMoment(moment?);
    private generateCalendar();
}
