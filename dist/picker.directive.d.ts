import { ElementRef, ViewContainerRef, EventEmitter, OnInit, OnChanges, SimpleChanges, ComponentFactoryResolver, OnDestroy } from '@angular/core';
import { ControlValueAccessor } from '@angular/forms';
export declare const PICKER_VALUE_ACCESSOR: any;
export declare class DateTimePickerDirective implements OnInit, OnChanges, OnDestroy, ControlValueAccessor {
    private vcRef;
    private componentFactoryResolver;
    private el;
    onChange: EventEmitter<any>;
    onError: EventEmitter<any>;
    autoClose: boolean;
    disabled: boolean;
    locale: string;
    viewFormat: string;
    returnObject: string;
    mode: 'popup' | 'dropdown' | 'inline';
    hourTime: '12' | '24';
    minMoment: string;
    maxMoment: string;
    position: 'top' | 'right' | 'bottom' | 'left';
    positionOffset: string;
    pickerType: 'both' | 'date' | 'time';
    showSeconds: boolean;
    onlyCurrentMonth: boolean;
    onModelChange: Function;
    onModelTouched: Function;
    private created;
    private dialog;
    private container;
    constructor(vcRef: ViewContainerRef, componentFactoryResolver: ComponentFactoryResolver, el: ElementRef);
    ngOnChanges(changes: SimpleChanges): void;
    ngOnInit(): void;
    ngOnDestroy(): void;
    writeValue(obj: any): void;
    registerOnChange(fn: any): void;
    registerOnTouched(fn: any): void;
    setDisabledState(isDisabled: boolean): void;
    onClick(): void;
    momentChanged(value: any): void;
    sendError(error: any): void;
    dispose(): void;
    private generateComponent();
    private openDialog();
}
